/**
 * 数据库服务模块
 * 
 * 提供数据库连接和操作功能
 * 演示了模块封装和信息隐藏的概念
 */

// 私有变量，存储数据库密码，由于未被导出，外部模块无法访问
const privatePassword = 'secret123';  // 私有变量，外部无法访问

/**
 * 私有函数，处理数据库连接
 * 由于未被导出，外部模块无法直接调用
 */
function connect() {
    // 内部实现细节
    // 这里应该包含实际的数据库连接逻辑
}

// 只导出需要公开的接口，隐藏实现细节
module.exports = {
    /**
     * 执行 SQL 查询
     * @param {string} sql - SQL 查询语句
     * 首先建立连接，然后执行查询操作
     */
    query: function(sql) {
        connect();
        // 执行查询
        // 这里应该包含实际的查询执行逻辑
    },
    
    /**
     * 插入数据
     * @param {Object} data - 要插入的数据对象
     * 首先建立连接，然后执行插入操作
     */
    insert: function(data) {
        connect();
        // 执行插入
        // 这里应该包含实际的数据插入逻辑
    }
};